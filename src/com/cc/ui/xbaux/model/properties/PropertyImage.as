package com.cc.ui.xbaux.model.properties
{
	import flash.display.DisplayObject;
	import flash.display.DisplayObjectContainer;
	
	public class PropertyImage extends Property
	{
		// so far, PropertyImage is expected to do very basic things, just like the base class Property;
		// however, we'd prefer to keep this class.
	}
}